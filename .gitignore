# Dependency directories
node_modules/
jspm_packages/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Environment variables & secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# Operating System files
.DS_Store
Thumbs.db

# IDEs and Editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Testing
coverage/
.nyc_output

# Overnight headless coder runtime state (queue + logs are local-only)
night-queue/

# Local Claude Code state
.claude/

# Runtime persistence (P1 JSON snapshots of galaxy + players)
data/
# Test-only persistence scratch dirs (RedisStore/multinode/restart integration)
data-test-*/

# Vitest browser visual testing attachments
.vitest-attachments/

# Persistent security audit json ledger and transient sandbox worktrees
plan/security_audit.json
plan/*_runner_test.json
.sandbox-worktrees/
.sandbox-worktrees-test/
.sandbox-runner-test-dir/

# Guest-sandbox runner scratch fixtures written during tests
src/net/temp_guest_*
src/net/temp_cli_*

# Active loop lockfile
plan/loop_active.lock

# Agent execution metadata and logs
.agents/

# Generated artifacts (rebuilt by `npm run codex:generate` and the test suites)
plan/CODEX.md
plan/codex.json
plan/monitoring_report.json
docs/ai/REPO_MAP.md
browser_recordings/

