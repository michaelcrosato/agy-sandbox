# BACKLOG — adjacent ideas surfaced during execution

Items noticed mid-spec that are out of the current spec's scope. Triage into `specs/` when prioritized.

Currently, **zero** items are active in the backlog. All previously tracked items (NPC Smuggler fleets, dynamic trade profit AI perception, stargate navigation HUD) have been fully implemented in Wave v19.
