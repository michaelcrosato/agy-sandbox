# Local Mock Dry Run Feature

This is a local dry-run issue to test the autonomous coder agent loop.
Please add a simple comment or dummy variable (e.g. `const MOCK_DRY_RUN = true;`) inside `src/server.js` or another source file, ensure it formats and passes all unit tests, and commit the result.
