{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": false,
    "noImplicitAny": false,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "types": ["node"]
  },
  "_comment": "spec 024 checkJs gate, phase 1 scope: the import-isolated net/physics/server modules (hardening, observability, physics, room lifecycle). The stateful engine classes need JSDoc-typedef work for their untyped {...parentParams} configs before they can join the checked graph — tracked in plan/BACKLOG.md to ratchet up.",
  "include": [
    "src/net/**/*.ts",
    "src/physics/**/*.ts",
    "src/server/**/*.js",
    "src/engine/**/*.ts",
    "src/persistence/**/*.ts"
  ],
  "exclude": ["node_modules", "**/*.test.js", "**/*.test.ts"]
}
